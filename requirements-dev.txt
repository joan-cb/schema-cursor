# Development dependencies for JSON Schema Builder & Annotator
genson>=1.2.2          # JSON schema generation
tabulate>=0.9.0        # Table formatting for CLI mode
pytest>=7.0.0          # Unit testing framework
textual>=0.45.0        # Terminal User Interface framework
